@model TechnicalInterview_FeedReader.Models.RegisterModel
@{
    ViewBag.Title = "Register";
}

<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
    <h2>Create a new account.</h2>
</hgroup>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary()

    <fieldset>
        <legend>Registration Form</legend>
        <ol>
            <li>
                @Html.LabelFor(RSSModel => RSSModel.UserName)
                @Html.TextBoxFor(RSSModel => RSSModel.UserName)
            </li>
            <li>
                @Html.LabelFor(RSSModel => RSSModel.Password)
                @Html.PasswordFor(RSSModel => RSSModel.Password)
            </li>
            <li>
                @Html.LabelFor(RSSModel => RSSModel.ConfirmPassword)
                @Html.PasswordFor(RSSModel => RSSModel.ConfirmPassword)
            </li>
        </ol>
        <input type="submit" value="Register" />
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
