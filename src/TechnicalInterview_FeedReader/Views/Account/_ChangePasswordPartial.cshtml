@model TechnicalInterview_FeedReader.Models.LocalPasswordModel

<h3>Change password</h3>

@using (Html.BeginForm("Manage", "Account")) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary()

    <fieldset>
        <legend>Change Password Form</legend>
        <ol>
            <li>
                @Html.LabelFor(RSSModel => RSSModel.OldPassword)
                @Html.PasswordFor(RSSModel => RSSModel.OldPassword)
            </li>
            <li>
                @Html.LabelFor(RSSModel => RSSModel.NewPassword)
                @Html.PasswordFor(RSSModel => RSSModel.NewPassword)
            </li>
            <li>
                @Html.LabelFor(RSSModel => RSSModel.ConfirmPassword)
                @Html.PasswordFor(RSSModel => RSSModel.ConfirmPassword)
            </li>
        </ol>
        <input type="submit" value="Change password" />
    </fieldset>
}
